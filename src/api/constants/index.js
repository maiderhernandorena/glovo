const { API_URL, API_VERSION, API_KEY } = require('./Api')
const { KEYS, VALUES, ATTRIBUTES } = require('./Data')

module.exports = {
    API_URL,
    API_VERSION,
    API_KEY,
    
    KEYS,
    VALUES,
    ATTRIBUTES
} 

